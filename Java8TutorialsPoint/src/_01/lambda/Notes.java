package _01.lambda;

public class Notes {

}

// parameter -> expression body

// ### Optional type declaration
// No need to declare the type of a parameter.
// The compiler can inference the same from the value of the parameter

// ### Optional parenthesis around paramameter
// No need to declare a single parameter in parantheses. for multiple parameter,
// parantheses are required.

// ####optional curly braces
// no need to use curly braces in expression body if the body constains a single
// statement;

// #### optional return keyword
// the compiler automatically return the value if the bod has a singke
// expression to return value;