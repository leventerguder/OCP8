package ch22;

public class QX15 {
	// What happens if you run this code with no resource bundles available?
	
	// ResourceBundle rb = ResourceBundle.getBundle("ghost.None");

	// A. It creates a resource bundle with no key/value pairs.
	// B. It runs without issue because the resource bundle is only searched for
	// when getting the
	// first key.
	// C. It throws a MissingResourceException. (+)
	// D. None of the above
}
