package ch23;

public class QX16 {

}

// Which of the following statements about InputStream and Reader are correct? (Choose two.)

// A. One contains a read() method that returns a byte value, while the other
// contains a read() method that returns an int value.
// B. Only one of them contains a flush() method.
// C. Only one of them contains a skip() method.
// D. They are both abstract classes. (+)
// E. They are both interfaces.
// F. They can both be used to read character data. (+)