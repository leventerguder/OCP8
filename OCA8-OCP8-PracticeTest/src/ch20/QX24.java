package ch20;

public class QX24 {

}

// Which statement about parallel streams is correct?

// A. A parallel stream always executes all stream operations faster than a
// serial stream.
// B. A parallel stream always executes certain stream operations faster than a
// serial stream.
// C. A parallel stream synchronizes its operations so that they are atomic.
// D. All streams can be converted to a parallel stream. (+)