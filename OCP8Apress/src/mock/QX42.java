package mock;

import java.util.Locale;

public class QX42 {
	public static void main(String[] args) {

		// Locale locale1 = new Locale.US;
		Locale locale2 = Locale.US;
		// Locale locale3 = new US.Locale();
		// Locale locale4 = Locale("US");
		// Locale locale5 = new Locale(Locale.US);
	}
}
