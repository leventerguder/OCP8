package mock;

public class QX12 {
	// Which of the following statements are true with respect to enums? (Select all
	// that apply.)
	// a) An enum can have private constructor (true)
	// b) An enum can have public constructor (false)
	// c) An enum can have public methods and fields (true)
	// d) An enum can implement an interface (true)
	// e) An enum can extend a class (false)
}
